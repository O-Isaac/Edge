<style is:inline>
  .content-panel {
    position: relative;
  }

  .content-panel > * {
    isolation: isolate;
  }

  .content-panel::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 500px;
    background: url(/assets/images/minified/hero-bg.webp) top left/cover;
    mask-image: linear-gradient(to bottom, black -30%, transparent);
  }
</style>
