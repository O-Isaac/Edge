<style is:inline>
    .content-panel {
        position: relative;
    }

    .content-panel > * {
        isolation: isolate;
    }

    .content-panel::before {
        content: "";
        
        position: absolute;
        top: 0;
        left: 0;

        width: 100%;
        height: 500px;
        
        background: var(--hero-bg);
    }
</style>